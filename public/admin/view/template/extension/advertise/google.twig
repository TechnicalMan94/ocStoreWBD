{{ header }}
{{ column_left }}
<div id="content">
    <div class="page-header">
        <div class="container-fluid">
            <div class="float-end">
                <button type="submit" class="btn btn-primary" form="form" data-bs-toggle="tooltip" title="{{ button_save }}"><i class="bi bi-floppy"></i></button>
                <a href="{{ mapping }}" class="btn btn-warning" data-bs-toggle="tooltip" title="{{ button_mapping }}"><i class="bi bi-tags"></i></a>
                <a href="{{ shipping_taxes }}" class="btn btn-warning" data-bs-toggle="tooltip" title="{{ button_shipping_taxes }}"><i class="bi bi-truck"></i></a>
                <a href="{{ campaign }}" class="btn btn-warning" data-bs-toggle="tooltip" title="{{ button_campaign }}"><i class="bi bi-gears"></i></a>
                <a href="{{ text_video_tutorial_url_setup }}" target="_blank" class="btn btn-info" data-bs-toggle="tooltip" title="{{ button_video_tutorial_setup }}"><i class="bi bi-camera-video"></i></a>
                <a href="{{ cancel }}" class="btn btn-light" data-bs-toggle="tooltip" title="{{ button_cancel }}"><i class="bi bi-arrow-left"></i></a>
            </div> 
            <h1>{{ heading_title }}</h1>
            <ul class="breadcrumb">
                {% for breadcrumb in breadcrumbs %}
                <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>
    <div class="container-fluid">
        {% if success %}
            <div class="alert alert-success alert-dismissible" role="alert">
                <button type="button" class="close" data-bs-dismiss="alert" aria-label="{{ text_close }}"><i class="bi bi-x-lg"></i></button>
                <i class="bi bi-check-circle"></i>&nbsp;{{ success }}
            </div>
        {% endif %}
        <div id="blockerError" style="display: none;" class="alert alert-danger">
            <i class="bi bi-exclamation-triangle"></i>&nbsp;{{ error_adblock }}
        </div>
        {% if error %}
            <div class="alert alert-danger alert-dismissible" role="alert">
                <button type="button" class="close" data-bs-dismiss="alert" aria-label="{{ text_close }}"><i class="bi bi-x-lg"></i></button>
                <i class="bi bi-exclamation-triangle"></i>&nbsp;{{ error }}
            </div>
        {% endif %}
        <div id="warning-container">
            {% if warning %}
                <div class="alert alert-warning alert-dismissible" role="alert">
                    <button type="button" class="close" data-bs-dismiss="alert" aria-label="{{ text_close }}"><i class="bi bi-x-lg"></i></button>
                    <i class="bi bi-info-circle"></i>&nbsp;{{ warning }}
                </div>
            {% endif %}
        </div>
        <div id="alert-container"></div>
        <div class="card">
            <div class="card-header">
                <h3 class="card-title mb-0"><i class="bi bi-pencil-square"></i>&nbsp;<span>{{ text_panel_heading }}</span></h3>
            </div>
            <div class="card-body">
                <form action="{{ action }}" method="post" enctype="multipart/form-data" id="form">
                    <div class="tabbable">
                        <ul class="nav nav-tabs mainMenuTabs">
                            <li class="active"><a href="#tab_ads" data-bs-toggle="tab">{{ tab_text_ads }}</a></li>
                            <li><a href="#tab_reports" data-bs-toggle="tab">{{ tab_text_reports }}</a></li>
                            <li><a href="#tab_settings" data-bs-toggle="tab">{{ tab_text_settings }}</a></li>
                        </ul>
                        <div class="tab-content">
                            <div class="tab-pane" id="tab_settings">{{ tab_settings }}</div>
                            <div class="tab-pane active" id="tab_ads">{{ tab_ads }}</div>
                            <div class="tab-pane" id="tab_reports">{{ tab_reports }}</div>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
<style type="text/css">
    .adBanner {
        background-color: transparent;
        height: 1px;
        width: 1px;
    }
</style>
<div id="wrapTest">
    <div class="adBanner">
    </div>
</div>
<script>
(function($) {
    $(document).ready(function() {
        $('#blockerError').toggle($("#wrapTest").height() == 0);
    });
})(jQuery);
</script>
{{ footer }}